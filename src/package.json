{
  "name": "moby-counter",
  "private": true,
  "version": "1.0.0",
  "description": "A simple app to demonstrate docker plugins for weave and flocker",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
